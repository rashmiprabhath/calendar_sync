CREATE TABLE EVENTS (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    TITLE VARCHAR2(255),
    DESCRIPTION VARCHAR2(1000),
    START_TIME TIMESTAMP,
    END_TIME TIMESTAMP,
    TIMEZONE VARCHAR2(100),
    SYNCED_WITH_CALENDAR NUMBER(1) DEFAULT 0
);
